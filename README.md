its just windows in a browser. open an issue for any bugs
NOTE: .wcdocs can be malicious files, they can run JS. Be careful.
To update: click Start (the ⊞ button), then click the power button (⏻) in the bottom-right corner of the Start menu. That opens the power menu — when a new version is available, "Update and restart" shows up at the top (with an orange dot on the power button as the hint).
